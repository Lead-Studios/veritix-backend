/**
 * BlockchainAnchorType Enum
 * Defines the different types of data that can be anchored to the blockchain
 */
export enum BlockchainAnchorType {
  EVENT = 'EVENT',
  TICKET = 'TICKET',
  TICKET_TRANSFER = 'TICKET_TRANSFER',
  EVENT_VERIFICATION = 'EVENT_VERIFICATION',
}
