/**
 * BlockchainAnchorStatus Enum
 * Tracks the status of blockchain anchoring operations
 */
export enum BlockchainAnchorStatus {
  PENDING = 'PENDING',
  ANCHORED = 'ANCHORED',
  FAILED = 'FAILED',
  VERIFIED = 'VERIFIED',
}
