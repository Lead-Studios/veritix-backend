/**
 * BlockchainProvider Enum
 * Supported blockchain providers for anchoring operations
 */
export enum BlockchainProvider {
  STELLAR = 'STELLAR',
  // Future providers can be added here
  // ETHEREUM = 'ETHEREUM',
  // POLYGON = 'POLYGON',
}
